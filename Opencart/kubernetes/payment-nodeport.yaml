apiVersion: v1
kind: Service
metadata:
  name: payment-service-nodeport
spec:
  type: NodePort
  selector:
    app: payment-service
  ports:
    - port: 6000
      targetPort: 6000
      nodePort: 31600
